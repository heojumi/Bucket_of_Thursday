import React from 'react';
import {Link} from 'react-router-dom';

const Bucket = (props) =>{
    return(
        <div>
            <h2>{props.title}</h2>
        </div>
    );
}

export default Bucket;